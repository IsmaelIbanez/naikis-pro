{
  "name": "naikis-pro",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel serve src/index.html",
    "deploy": "rm -rf build && parcel build -d build --public-url /naikis-pro/  && gh-pages -d build"
  },
  "author": "Isma",
  "license": "ISC",
  "keywords": [],
  "description": "",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4"
  }
}
